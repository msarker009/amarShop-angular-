export interface orderInfo {
  email: string;
  address: string;
  contact: string;
}
export interface orderDetails {
  email: string;
  address: string;
  contact: string;
  totalPrice: number;
  userId: number;
  id: number | undefined;

}
